{{>header}}
{{>navbar}}
    <section class="container p-3 my-4 ">
        <div class="row">
        {{#each data}}  
            <div class="col-4">
                <a href="{{this.id}}">
                    <img class="img-fluid" src="{{this.url}}">
                </a>
            </div>
        {{/each}}
        </div>
    </section>
{{>footer}}