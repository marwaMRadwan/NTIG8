import { XDirectiveDirective } from './x-directive.directive';

describe('XDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new XDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
